import 'package:flutter/material.dart';

class Themeclass {
  static ThemeData dark =
      ThemeData.dark().copyWith(scaffoldBackgroundColor: Colors.red);
  static ThemeData light =
      ThemeData.light().copyWith(scaffoldBackgroundColor: Colors.blue);
}
